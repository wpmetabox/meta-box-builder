import React from 'react';
import Checkbox from '../Checkbox';

const CloneDefault = props => <Checkbox {...props} className='clone_optional' label="Clone default value" tooltip="Allows to drag-and-drop reorder clones" />
export default CloneDefault;